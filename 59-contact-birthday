public Boolean isBirthday(Contact c) {
    //code here
    Date birthDate =c.Birthdate; 
    if(birthDate.dayOfYear() == date.today().dayOfYear()){
        return true;
    }else{
        return false;
    }
}