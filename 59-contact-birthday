public Boolean isBirthday(Contact c) {
    //code here
    Date birthDate =c.Birthdate; 
    if(birthDate.day() == System.today().day() && birthDate.month()==System.today().day()){
        return true;
    }else{
        return false;
    }
}
