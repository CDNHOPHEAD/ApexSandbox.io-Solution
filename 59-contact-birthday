public Boolean isBirthday(Contact c) {
    //code here
    Date birthDate =c.Birthdate; 
    if(birthDate.dayOfYear() == System.today().dayOfYear()){
        return true;
    }else{
        return false;
    }
}