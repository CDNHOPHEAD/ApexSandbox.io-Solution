public String changeTimeFormat(String strTime) {
    //code here
    Integer hour = Integer.valueOf(strTime.substringBefore(':'));
    String minutes = strTime.substringAfter(':');
    String formattedTime;

    if(hour == 0){
        hour = 12;
        formattedTime = String.valueOf(hour)+':'+minutes+' AM';
    }else if(hour > 12){
        hour -= 12;
        formattedTime = String.valueOf(hour)+':'+minutes+' PM';
    }else{
        formattedTime = strTime+' AM';
    }
    return formattedTime;
}