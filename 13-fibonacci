public Integer fibonacci(Integer n) {
    //code here
    List<Integer> numList = new List<Integer>();
    
    if(n==1 || n==2){
            numList.add(1);
    }else{
        for(Integer i=1;i < n; i++){
        Integer firstNum = numList.get(i-2);
        Integer secondNum = numList.get(i-1);
        Integer currentNum = firstNum + secondNum;
        numList.add(currentNum);
        }
    }
    Integer size = numList.size();
    Integer fib = numList.get(size);
    return fib;
}